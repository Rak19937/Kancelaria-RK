RK KANCELARIA 0.3.0-dev3 — TESTY

1. Kompilacja app.py, rk_smart.py, desktop.py, rk_paths.py, rk_sync.py i wrappera — OK.
2. TEST_TRUE_PORTABLE_PATHS.py — OK.
3. Self-test wrappera desktopowego — OK (Qt nie jest wymagany do testu źródeł na Linux).
4. Inicjalizacja pustej bazy — schema 320, integrity_check=ok — OK.
5. Migracja symulowanej schema 310 -> 320 zachowała liczbę spraw, zadań,
   dokumentów i zdarzeń — OK.
6. Utworzenie wszystkich siedmiu nowych struktur danych — OK.
7. HTTP smoke test jedenastu zakładek sprawy — OK.
8. Zapis i ponowny odczyt strategii, twierdzenia, roszczenia i historii — OK.
9. Pamiętanie ostatniej zakładki użytkownika dla sprawy — OK.
10. Rejestr roszczeń: 27 500,57 PLN bez utraty groszy — OK.
11. Test odtworzenia pełnego snapshotu: integralność bazy i kompletność plików — OK.

Do wykonania przez GitHub Actions / Windows:
- build PyInstaller,
- self-test gotowego EXE,
- kontrola WebEngine na Windows,
- publikacja ZIP Windows Portable.
