RK KANCELARIA 0.3.0-dev1 — TESTY

1. python -m py_compile app.py rk_smart.py desktop.py rk_paths.py rk_sync.py RK_KANCELARIA_DESKTOP.pyw — OK
2. RK_KANCELARIA_DESKTOP.pyw --self-test — SELFTEST OK (Qt niedostępny w środowisku Linux, kod powłoki bez błędu)
3. TEST_TRUE_PORTABLE_PATHS.py — OK
4. Migracja schema 240 DEV9 → schema 300 — OK; stare rekordy zachowane.
5. HTTP: /, /calendar, /law, /case/<id>, /case/<id>?mode=advanced — 200 OK.
6. Alert/monit — zapis i widoczność w bazie — OK.
7. Smart links — ręczne relacje i relacje automatyczne — OK.
8. Termin jawny w treści „w terminie 14 dni” — powstała sugestia, nie zadanie — OK.
9. Dopiero po akceptacji sugestii utworzono zadanie/termin i relację „uruchamia termin” — OK.
10. Dokument bez jawnego okresu — 0 sugestii i 0 automatycznych terminów — OK.
11. Parser biblioteki prawa — import przykładowego HTML, wyszukiwanie art. 2 i pełnotekstowe — OK.
12. ELI w środowisku testowym: brak DNS; błąd obsłużony, istniejąca wersja lokalna nieusunięta — OK.

Do testu na Windows po buildzie GitHub Actions:
- render PySide6/WebEngine,
- pakiet EXE,
- pobranie rzeczywistych tekstów ELI przy aktywnym Internecie.
